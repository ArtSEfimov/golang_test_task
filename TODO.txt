📌 Подсказка: как работает IndexMap

- IndexMap — это отображение RecordID → Location, где Location содержит координаты (смещение и размер) записи в файле.
- При добавлении новой записи создаётся новый RecordID, и в IndexMap добавляется новая пара.
- При обновлении существующей записи с тем же RecordID:
  • Новая версия записывается в конец файла.
  • IndexMap[RecordID] обновляется на новые координаты.
- При удалении записи:
  • Просто выполняется delete(IndexMap, RecordID).
  • (Опционально) в файл можно записать "tombstone", чтобы компактор мог позже удалить физически.
- Таким образом, в IndexMap всегда содержится только актуальное состояние данных.
- Старые версии остаются в файле, но не мешают работе — они очищаются при уплотнении (компакт).
